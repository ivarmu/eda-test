---
- name: Check webserver availability
  hosts: all
  sources:
    - ansible.eda.url_check:
        urls:
          - https://google.com
        delay: 60

  rules:
    - name: Site is down
      condition: event.url_check.status == "down"
      action:
        debug:
          msg: "DOWN: {{ event.url_check.url }}"
...
